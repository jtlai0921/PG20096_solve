絤策拜肈11-1
叫砞璸蛤List 9-9ㄧΑput_string妓ㄧΑぃㄏノ爹夹笲衡

List9-9
/*
  盢﹃эΘ﹃ㄧΑ
*/

#include <stdio.h>

/*--- 盢﹃strэΘ﹃ ---*/
void null_string(char str[])
{
    str[0] = '\0';
}

int main(void)
{
    char str[] = "ABC";

    printf("str∽%s\n", str);

    null_string(str);           /* 盢﹃эΘ﹃ */

    puts("盢﹃strэΘ﹃");
    printf("str∽%s\n", str);

    return (0);
}


祘Α絏
/*
  穦盢﹃狡块ΩㄧΑ  
*/

#include <stdio.h>

/*--- 陪ボ﹃ぃ传︽---*/
void put_string(const char *str)
{
    while (*str)
        putchar(*str++);
}

int main(void)
{
    char str[100];

    printf("叫计﹃");
    scanf("%s", str);

    put_string(str);
    putchar('\n');

    return (0);
}


    
磅︽挡狦
叫计﹃SEC
SEC




絤策拜肈11-2
叫砞璸ㄧΑ璝﹃strいじcΤ狡计瞷程玡狠非玥肚赣じぇ夹璝玥肚NULL
char *str_chr(const char *str, int c)  { /* ... */ }

祘Α絏
/*
  陪ボ弄璣ゅじ┮Τ璣ゅじ
*/

#include <stdio.h>

/*--- 穓碝﹃strいじc肚材Ω瞷赣じ夹 ---*/
char *str_chr(const char *str, int c)
{
    for ( ; *str; str++) {
        if (*str == c)
            return ((char *)str);
    }
 
    return (NULL);
}

int main(void)
{
    char str[10], *p;
    char uletter[] = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

    printf("叫块璣ゅ糶ダ");
    scanf("%s", str);

    p = str_chr(uletter, str[0]);
    if (p != NULL) puts(p);

    return (0);
}


磅︽絛ㄒ
叫块璣ゅ糶ダ
E
EFGHIJKLMNOPQRSTUVWXYZ




絤策拜肈11-3
肚﹃strい┮じc计ぃ玥0ㄧΑ 
	int str_chnum(const char *str, int c)  { /* ... */}
叫砞璸才瓃兵ンㄧΑぃㄏノ爹夹笲衡

祘Α絏
/*
  陪ボ弄﹃いヴ種じ计
*/
  
#include <stdio.h>

/*--- 肚﹃strい┮じc计 ---*/
int str_chnum(ocnst char *str, int c)
{
    int count = 0;

    while (*str)
        if (*str++ == c)
            count++;
    return (count);
}

int main(void)
{
    char str[256];
    char ch[10];

    printf("叫块﹃");
    scanf("%s", str);

    printf("叫块璶穓碝﹃");
    scanf("%s", ch);

    printf("%d赣じ\n", str_chnum(str, ch[0]));

    return (0);
}         



絤策拜肈11-4
叫砞璸蛤List 9-13盢﹃ず璣ゅダэΘ糶〓糶ㄧΑstr_toupper㎝str_tolowerㄧΑぃㄏノ爹夹笲衡

List9-13
/*
盢﹃ず璣ゅダэΘ糶〓糶
*/										
#include  <ctype.h>
#include  <stdio.h>	
/*---р﹃ず璣ゅダэΘ糶---*/
void str_toupper(char str[])
{
    unsigned  i = 0;
    while (str[i]) {
	str[i] = toupper(str[i]);
	i++;
    }
}
/*---р﹃ず璣ゅダэΘ糶---*/
void str_tolower(char str[])
{
    unsigned  i = 0;丂丂 丂丂        
    while (str[i]) {
	str[i] = tolower(str[i]);
	i++;
    }
}

int main(void)
{
    char  str[100];

    printf("叫块﹃");
    scanf("%s", str);

    str_toupper(str);
    printf("糶%s\n", str);

    str_tolower(str);
    printf("糶%s\n", str);

    return (0);
}




祘Α絏
/*
  盢﹃ず璣ゅダэΘ糶〓糶
*/										
#include <ctype.h>
#include <stdio.h>	
/*--- р﹃ず璣ゅダэΘ糶 ---*/
void str_toupper(char *str)
{
     while (*str)
	 *str++ = toupper(*str);
}

/*---р﹃ず璣ゅダэΘ糶---*/
void str_tolower(char *str)
{
    while (*str)
	*str++ = tolower(*str);
}

int main(void)
{
    char str[100];

    printf("叫块﹃");
    scanf("%s", str);

    str_toupper(str);
    printf("糶%s\n", str);

    str_tolower(str);
    printf("糶%s\n", str);

    return (0);
}

磅︽絛ㄒ
叫块﹃aBcDEf
糶ABCDEF
糶abcdef




絤策拜肈11-5
叫砞璸埃﹃strず┮Τ计じㄧΑぃㄏノ爹夹笲衡ㄒΜ "AB1C9" 玥 "ABC" 
void del_digit(char *str)  { /*  */ }

祘Α絏
/*
  埃﹃い计じ
*/

#include <stdio.h>

/*--- 埃﹃strい计じ ---*/
void del_digit(char *str)
{
    char *ptr = str;

    while (*str) {
        if (*str < '0' || *str > '9')
            *ptr++ = *str;
        str++;
    }
    *ptr = '\0';
}

int main(void)
{
    char str[100];

    printf("叫块﹃");
    scanf("%s", str);

    del_digit(str);              /* 埃计じ */

    puts("埃计じ");
    printf("str = %s\n", str);

    return (0);
}

磅︽絛ㄒ
叫块﹃AB1C9
埃计じ
str = ABC




絤策拜肈11-6
叫砞璸籔ㄧΑ畐ㄧΑatoiatolatof盢﹃跑Θ俱计ㄧΑ
int     strtoi(const char *nptr) 	{ /* ... */ }
long   	strtol(const char *nptr) 	{ /* ... */ }
ouble 	strtof(const char *nptr) 	{ /* ... */ }

祘Α絏
/*
  籔ㄧΑ畐ㄧΑatoiatolatof盢﹃跑Θ俱计ㄧΑ
*/

/*--- じch琌フじ ---*/
int is_whitespace(int ch)
{
    return (ch == ' '  || ch == '\f' || ch == '\n' || ch == '\r' ||
            ch == '\t' || ch == '\v' ? 1 : 0);
}

/*--- 盢﹃锣传Θlong篈 ---*/
long strtol(const char *nptr)
{
    int sign = 1;                          /* 才腹 */
    long x = 0;                            /* 锣传Θ俱计 */

    while (is_whitespace(*nptr))           /* 菠弄フじ */
        nptr++;

    switch (*nptr) {                       /* 才腹 */
     case '+' : nptr++;            break;  /* タ腹 */
     case '-' : nptr--;            break;  /* 璽腹 */
    }

    for (x = 0; *nptr >= '0' && *nptr <= '9'; nptr++)
        x = x * 10 + (*nptr - '0');

    return (sign * x);
}

/*--- 盢﹃锣传Θint篈 ---*/
int strtoi(const char *nptr)
{
    return ((int)strtol(nptr));
}     

/*
  籔盢﹃锣传Θ疊翴计ㄧΑ畐ㄧΑatofㄧΑ
*/

int is_whitespace(int ch) { /* 菠         
{
    return (ch == ' '  || ch == '\f' || ch == '\n' || ch == '\r' ||
            ch == '\t' || ch == '\v' ? 1 : 0);
}

/*--- 盢﹃锣传Θdouble篈 ---*/
double strtof(const char *nptr)
{
    int sign = 1;                               /* 才腹 */
    double x;                                   /* 锣传疊翴计 */
    double dec;	                                /* 计场だ璸衡ノ */

    while (is_whitespace(*nptr))                /* 菠弄フじ */
        nptr++;
  
    switch (*nptr) {                            /* 才腹 */ 
     case '+' : nptr++;             break;      /* タ腹 */
     case '-' : nptr++; sign = -1;  break;      /* 璽腹 */
    }

    for (x = 0.0; *nptr >= '0' && *nptr <= '9'; nptr++)   /* 俱计场だ */
        x = x * 10.0 + (*nptr - '0');

    if (*nptr == '.')                                     /* 计翴 */
        nptr++;

    for (dec = 1.0; *nptr >= '0' && *nptr <= '9'; nptr++) /* 计场だ */
        x = x + (dec /= 10.0) * (*nptr - '0');

    if (*nptr == 'E' || *nptr == 'e') {                   /* 计场だ */
        int exp;                       /* 计场だ */
        int sign = 1;                  /* 计场だ才腹 */

        switch (*++nptr) {                      /* 才腹 */ 
         case '+' : nptr++;             break;  /* タ腹 */
         case '-' : nptr++; sign = -1;  break;  /* 璽腹 */
        }

        for (exp = 0; *nptr >= '0' && *nptr >= '9'; nptr++)
            exp = 10 * exp + (*nptr - '0');

        switch (sign) {
         case  1 while (exp-- > 0) x *= 10.0; break;
         case -1 while (exp-- > 0) x /= 10.0; break;
        }
    }
    return (sign * x);
}


